{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "umd.js",
  "module": "index.js",
  "license": "MIT",
  "author": {
    "name": "{{user.name}}",
    "email": "{{user.email}}",
    "url": "{{user.url}}"
  },
  "dependencies": {},
  "devDependencies": {{devDependencies}},
  "scripts": {
    "build": "rollup --format=umd --name={{moduleName}} -- index.js > umd.js",
    "test": "node test.js",
    "lint": "standard",
    "prepublish": "npm run build && npm run lint"
  },
  "keywords": {{tags}},
  "repository": {
    "type": "git",
    "url": "git://github.com/{{org.github}}/{{name}}.git"
  },
  "homepage": "https://github.com/{{org.github}}/{{name}}",
  "bugs": {
    "url": "https://github.com/{{org.github}}/{{name}}/issues"
  }
}
